# 状态模式学习文档

## 📚 目录

1. [模式概述](#模式概述)
2. [问题场景](#问题场景)
3. [模式结构](#模式结构)
4. [代码实现](#代码实现)
5. [对比分析](#对比分析)
6. [优缺点](#优缺点)
7. [适用场景](#适用场景)
8. [JDK中的应用](#jdk中的应用)
9. [学习总结](#学习总结)

## 模式概述

### 定义
状态模式（State Pattern）允许一个对象在其内部状态改变时改变它的行为，对象看起来似乎修改了它的类。

### 核心思想
- **对象行为随状态改变**：同一操作在不同状态下有不同表现
- **状态转换封装**：将状态转换规则封装在状态类中
- **多态替代条件判断**：用虚方法调用替代switch/if-else语句

### 经典应用场景
- 订单状态管理（待支付→已支付→已发货→已完成）
- 游戏角色状态（正常→受伤→死亡）
- 交通工具状态（停止→行驶→维修）
- 工作流状态（起草→审核→批准→驳回）

## 问题场景

### 传统状态管理的问题

在没有使用状态模式之前，我们通常使用**枚举+switch**语句来管理状态：

```java
public enum OrderStatus {
    PENDING, PAID, SHIPPED, COMPLETED, CANCELLED
}

public class Order {
    private OrderStatus status;

    public boolean pay() {
        switch (status) {
            case PENDING:
                // 支付逻辑
                this.status = OrderStatus.PAID;
                return true;
            case PAID:
                System.out.println("已支付，请勿重复支付");
                return false;
            // ... 其他状态的处理
        }
    }
}
```

### 存在的问题

1. **代码重复**：每个方法都包含相同的switch结构
2. **违反开闭原则**：添加新状态需要修改所有方法
3. **职责不清**：一个类承担了所有状态的管理
4. **维护困难**：状态转换逻辑分散在多处
5. **容易出错**：手动维护状态转换容易产生不一致

## 模式结构

### 类图结构

```
+----------------+       uses          +----------------+
|    Context     | ------------------> |   State        |
| - state: State |                     | + handle()     |
| + request()    |                     +----------------+
+----------------+                          ▲
        ▲                                   |
        | implements                        | implements
        |                                   |
+----------------+       +----------------+ +----------------+
| ConcreteStateA |       | ConcreteStateB | | ConcreteStateC |
| + handle()      |      | + handle()     | | + handle()     |
+----------------+       +----------------+ +----------------+
```

### 角色说明

1. **Context（上下文）**
   - **持有一个State对象的引用**
   - 定义客户端感兴趣的接口
   - 将所有与状态相关的操作委托给State对象

2. **State（状态）**
   - 定义一个接口以封装与Context的一个特定状态相关的行为
   - 所有具体状态类都实现这个接口

3. **ConcreteState（具体状态）**
   - 实现State接口
   - 每个类封装一个状态的行为

## 代码实现

### 1. 状态接口（OrderState）

```java
public interface OrderState {
    // 核心方法
    boolean pay(Order order);
    boolean ship(Order order);
    boolean confirmDelivery(Order order);
    boolean cancel(Order order);

    boolean canPay();
    boolean canShip();
    boolean canCancel();

    String getStateName();
    String getStateDescription();
    String getAvailableActions();
}
```

### 2. 上下文类（Order）

```java
public class Order {
    // 持有一个State对象的引用
    private OrderState currentState;

    public Order(String orderId, double amount) {
        this.currentState = new PendingState();
    }

    // 业务方法委托给状态对象
    public boolean pay() {
        return currentState.pay(this);
    }

    public boolean ship() {
        return currentState.ship(this);
    }

    // 状态转换方法（重要，未来给具体的状态实现类调用实现“无感”切换）
    public void changeState(OrderState newState) {
        this.currentState = newState;
    }
}
```

### 3. 具体状态类示例（PendingState）

```java
public class PendingState implements OrderState {
    @Override
    public boolean pay(Order order) {
        System.out.println("处理支付...");
        order.changeState(new PaidState());
        return true;
    }

    @Override
    public boolean ship(Order order) {
        System.out.println("未支付，无法发货");
        return false;
    }

    @Override
    public boolean canPay() { return true; }
    @Override
    public boolean canShip() { return false; }

    // ... 其他方法实现
}
```

### 4. 完整状态转换图

```
    ┌─────────┐
    │ PENDING │
    └────┬────┘
         │ pay()
    ┌────▼────┐     cancel()     ┌────────────┐
    │  PAID   │ ───────────────▶ │ CANCELLED  │
    └────┬────┘                  └────────────┘
         │ ship()
    ┌────▼────┐
    │ SHIPPED │
    └────┬────┘
         │ confirm()
    ┌────▼────┐
    │COMPLETED│
    └─────────┘
```

## 对比分析

### 传统方式 vs 状态模式

| 对比项 | 传统方式 | 状态模式 |
|--------|----------|----------|
| **状态管理** | 枚举 + switch | 状态对象 + 多态 |
| **条件判断** | 大量if-else/switch | 虚方法调用 |
| **代码结构** | 单一类包含所有逻辑 | 多个小类各司其职 |
| **职责分离** | 职责集中，类过大 | 职责单一，类小而专注 |
| **扩展性** | 违反开闭原则 | **符合开闭原则** |
| **可维护性** | 修改影响多处 | 修改局部化 |
| **状态转换** | 逻辑分散在方法中 | 逻辑封装在状态类中 |

### 代码量对比

**传统方式的pay()方法：**
```java
public boolean pay() {
    switch (currentStatus) {
        case PENDING:
            updateStatus(OrderStatus.PAID);
            System.out.println("支付成功");
            return true;
        case PAID:
            System.out.println("已支付，请勿重复支付");
            return false;
        case SHIPPED:
            System.out.println("已发货，无需支付");
            return false;
        case COMPLETED:
            System.out.println("已完成，无需支付");
            return false;
        case CANCELLED:
            System.out.println("已取消，无法支付");
            return false;
        default:
            return false;
    }
}
```

**状态模式的pay()方法：**
```java
public boolean pay() {
    return currentState.pay(this);
}
```

## 优缺点

### ✅ 优点

1. **简化上下文类**
   - 消除了大量的条件判断语句
   - 上下文类变得更加简洁

2. **符合设计原则**
   - **开闭原则**：添加新状态无需修改现有代码
   - **单一职责原则**：每个状态类只负责一种状态的行为

3. **状态行为封装**
   - 每个状态的行为封装在独立的状态类中
   - 相关行为集中管理，易于理解和维护

4. **状态转换一致性**
   - 状态转换逻辑封装在状态类中
   - 转换规则集中管理，不易出错

5. **运行时灵活性**
   - 状态可以在运行时动态改变
   - 对象行为随状态改变而改变

### ❌ 缺点

1. **类数量增加**
   - 每个状态都需要一个类
   - 对于状态较少的场景可能过度设计

2. **复杂性增加**
   - 对于简单的状态管理可能显得复杂
   - 需要理解状态模式的设计思想

3. **状态间通信**
   - 状态类之间可能需要共享信息
   - 状态转换的逻辑可能比较复杂

## 适用场景

### ✅ 适用场景

1. **对象的行为依赖于它的状态**
   - 同一个操作在不同状态下有不同的行为
   - 状态数量相对稳定且有限

2. **状态转换规则复杂**
   - 状态转换逻辑比较复杂
   - 需要集中管理状态转换规则

3. **大量条件判断**
   - 代码中存在大量的switch/if-else语句
   - 这些条件判断都基于对象的状态

4. **需要动态改变行为**
   - 对象需要在运行时改变其行为
   - 行为的改变依赖于状态的改变

### ❌ 不适用场景

1. **状态数量极少**（1-2个状态）
2. **状态转换规则简单**
3. **状态很少改变**
4. **性能要求极高**（多态调用比直接调用稍慢）

## JDK中的应用

### 1. Thread类中的状态管理

```java
public enum State {
    NEW, RUNNABLE, BLOCKED, WAITING, TIMED_WAITING, TERMINATED;
}

// Thread类内部使用状态枚举管理线程状态
// 虽然不是标准的状态模式，但体现了状态管理的思想
```

### 2. java.util.Iterator

```java
public interface Iterator<E> {
    boolean hasNext();
    E next();
    default void remove() { throw new UnsupportedOperationException(); }
}

// 不同的Iterator实现有不同的行为
// 例如ArrayList.Itr和LinkedList.ListItr有不同的状态管理
```

### 3. java.io.InputStream

```java
public abstract class InputStream implements Closeable {
    // 不同状态的流（打开、关闭）有不同的行为
    public abstract int read() throws IOException;
}
```

## 学习总结

### 核心要点

1. **状态模式的本质**：将状态相关的行为封装到独立的状态类中

2. **与策略模式的区别**：
   - **状态模式**：对象有状态，行为随状态改变
   - **策略模式**：对象无状态，策略可以选择和替换

3. **实现要点**：
   - 上下文类持有状态对象的引用
   - 将状态相关操作委托给状态对象
   - 状态对象负责状态转换和具体行为

4. **设计原则体现**：
   - **开闭原则**：对扩展开放，对修改关闭
   - **单一职责原则**：每个状态类职责单一
   - **里氏替换原则**：状态对象可以互相替换

### 实践建议

1. **识别状态**：首先明确对象有哪些状态
2. **定义接口**：为所有状态定义统一的接口
3. **实现状态类**：为每个状态创建具体的实现类
4. **封装转换**：将状态转换逻辑封装在状态类中
5. **测试覆盖**：确保所有状态转换都被测试覆盖

### 扩展思考

1. **状态模式与状态机**：可以结合状态机图来设计状态模式
2. **状态持久化**：考虑如何将状态对象持久化到数据库
3. **状态监控**：可以添加状态变更的监听和日志
4. **性能优化**：对于频繁的状态转换，可以考虑对象池

---

通过本次学习，我们深入理解了状态模式的设计思想和实现方法。状态模式通过将状态相关的行为封装到独立的状态类中，使用多态替代条件判断，实现了代码的简化、职责的分离和系统的可扩展性。在实际开发中，当遇到复杂的状态管理需求时，状态模式是一个非常好的解决方案。