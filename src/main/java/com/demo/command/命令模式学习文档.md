# 命令模式学习文档

## 🎯 核心思想

命令模式的核心是：**引入"中间人"来实现解耦**

- **调用者**：只管发命令，不知道具体谁来执行
- **中间人**：具体命令类，负责调用具体的执行者
- **接收者**：最终要操作的对象（风扇、电灯）

```
调用者（遥控器） → 中间人（命令对象，不同命令持有自己要操作的不同的对象） → 接收者（家电）
     │                │                                          │
  按下按钮         调用具体方法                                   开灯/关灯
  "执行命令"        light.turnOn()                              实际工作
```

## 模式概述

命令模式（Command Pattern）是一种行为型设计模式，它将请求封装为对象，从而允许你用不同的请求对客户进行参数化，对请求排队或记录请求日志，以及支持可撤销的操作。

## 💡 角色解析（"中间人"视角）

### 1. 调用者（Invoker）- 指挥官
```java
public class RemoteControl {
    private Command[] onCommands;

    public void onButtonWasPressed(int slot) {
        onCommands[slot].execute();  // 只管调用命令，不关心具体是什么
    }
}
```
**特点**：
- 只知道调用`execute()`方法
- 完全不知道接收者是谁（电灯还是风扇）
- 实现了**调用者和接收者的完全解耦**

### 2. 中间人（ConcreteCommand）- 执行协调者
```java
public class LightOnCommand implements Command {
    private final Light light;  // 持有接收者引用

    @Override
    public void execute() {
        light.turnOn();  // 调用接收者的具体方法
    }
}
```
**特点**：
- 持有接收者的引用
- 知道**调用接收者的哪个方法**
- 实现`execute()`和`undo()`方法
- 是调用者和接收者之间的**桥梁**

### 3. 接收者（Receiver）- 真正的执行者
```java
public class Light {
    public void turnOn() { ... }    // 实际开灯逻辑
    public void turnOff() { ... }   // 实际关灯逻辑
}
```
**特点**：
- 包含真正的业务逻辑
- 知道如何完成具体操作
- 完全不知道命令模式的存在

## 模式结构

### 类图关系（中间人视角）

```
调用者（Invoker）      中间人（ConcreteCommand）    接收者（Receiver）
  RemoteControl  ←→   LightOnCommand          →      Light
       ↑                   ↑
   持有命令引用          持有接收者引用
       │                   │
  只调用execute()     调用接收者的具体方法
```

### 调用流程

```
1. 调用者按下按钮
   remote.onButtonWasPressed(0)

2. 中间人接收到执行请求（命令执行）
   lightOnCommand.execute()

3. 中间人调用接收者的具体方法（命令调用自己引用的底层接收者的相关方法）
   light.turnOn()

4. 接收者完成实际工作
   客厅灯已打开
```

## 代码示例分析

### 1. 命令接口 - Command

```java
public interface Command {
    void execute();  // 执行命令
    void undo();     // 撤销命令
}
```

**作用**：
- 定义命令对象的统一接口
- 所有具体命令都必须实现这个接口
- 支持执行和撤销操作

### 2. 接收者 - Receiver

```java
public class Light {
    public void turnOn() { ... }    // 开灯
    public void turnOff() { ... }   // 关灯
}

public class Fan {
    public void high() { ... }      // 高速
    public void medium() { ... }    // 中速
    public void low() { ... }       // 低速
    public void off() { ... }       // 关闭
}
```

**特点**：
- 真正执行工作的对象
- 知道如何实施与执行一个请求
- 与命令对象解耦

### 3. 具体命令 - ConcreteCommand

```java
public class LightOnCommand implements Command {
    private final Light light;

    public LightOnCommand(Light light) {
        this.light = light;
    }

    @Override
    public void execute() {
        light.turnOn();  // 委托给接收者
    }

    @Override
    public void undo() {
        light.turnOff();
    }
}
```

**核心作用**：
- 将一个接收者对象绑定到一个动作上
- 实现execute()和undo()方法
- 调用接收者的相应方法

### 4. 调用者 - Invoker

```java
public class RemoteControl {
    private Command[] onCommands;
    private Command[] offCommands;
    private Command undoCommand;

    public void onButtonWasPressed(int slot) {
        onCommands[slot].execute();
        undoCommand = onCommands[slot];  // 记录撤销命令
    }

    public void undoButtonWasPressed() {
        undoCommand.undo();
    }
}
```

**特点**：
- 持有命令对象
- 请求命令执行请求
- 支持撤销操作
- 完全不知道具体执行什么操作

### 5. 客户端 - Client

```java
// 创建接收者
Light light = new Light("客厅");

// 创建命令对象
Command lightOn = new LightOnCommand(light);
Command lightOff = new LightOffCommand(light);

// 设置调用者
RemoteControl remote = new RemoteControl();
remote.setCommand(0, lightOn, lightOff);

// 执行命令
remote.onButtonWasPressed(0);  // 开灯
remote.offButtonWasPressed(0); // 关灯
```

## 运行结果

```
=== 智能家居遥控器演示 ===


------ 遥控器 ------
[slot 0] LightOnCommand / LightOffCommand
[slot 1] LightOnCommand / LightOffCommand
[slot 2] FanHighCommand / FanOffCommand
[undo] NoCommand
-------------------

=== 开始测试遥控器 ===

1. 测试客厅灯：
客厅 灯已打开
客厅 灯已关闭
按撤销键：
客厅 灯已打开

2. 测试厨房灯：
厨房 灯已打开
厨房 灯已关闭

3. 测试风扇：
客厅 风扇设置为高速
按撤销键：
客厅 风扇已关闭

=== 测试未设置的按键 ===
该按键未设置命令
```

## 💡 命令模式的真正价值

### 🎯 核心价值：通过"中间人"实现解耦

```java
// ❌ 没有命令模式 - 紧耦合
class RemoteControl {
    private Light light;
    private Fan fan;

    public void pressLightButton() {
        light.turnOn();  // 直接操作接收者
    }

    public void pressFanButton() {
        fan.high();      // 直接操作接收者
    }
    // 遥控器必须知道所有的家电类型！
}

// ✅ 使用命令模式 - 完全解耦
class RemoteControl {
    private Command[] commands;

    public void pressButton(int slot) {
        commands[slot].execute();  // 只管调用命令，不关心是什么
    }
    // 遥控器完全不知道是什么家电！
}
```

### 🚀 关键优势

#### 1. **完全解耦**
- **调用者**：只管调用`execute()`，不知道接收者是谁
- **接收者**：只管干活，不知道被谁调用
- **中间人**：负责协调两边，知道如何调用

#### 2. **极大的灵活性**
- 可以在运行时动态设置命令
- 可以轻松添加新的家电类型
- 可以实现撤销、重做、日志等高级功能

#### 3. **职责分离**
- **调用者**：负责触发命令
- **中间人**：负责协调执行
- **接收者**：负责具体实现

#### 4. **支持撤销和重做**
- 通过记录执行过的命令，可以实现撤销操作
- 每个命令都可以实现自己的undo()逻辑

#### 5. **支持宏命令**
- 可以将多个命令组合成一个复合命令
- 实现批量操作

#### 6. **支持请求排队和日志**
- 命令对象可以被序列化，支持网络传输
- 可以实现操作的持久化和重放

#### 7. **易于扩展新命令**
- 添加新命令无需修改现有代码
- 遵循开闭原则

## 实际应用场景

### 1. GUI按钮和菜单
```java
// 按钮点击事件
Button saveButton = new Button();
saveButton.setCommand(new SaveCommand(document));
saveButton.click();  // 执行保存命令
```

### 2. 事务管理
```java
class TransactionManager {
    private List<Command> commands = new ArrayList<>();

    public void addCommand(Command command) {
        commands.add(command);
    }

    public void execute() {
        for (Command command : commands) {
            command.execute();
        }
    }

    public void rollback() {
        for (int i = commands.size() - 1; i >= 0; i--) {
            commands.get(i).undo();
        }
    }
}
```

### 3. 任务队列和线程池
```java
class TaskQueue {
    private Queue<Command> taskQueue = new LinkedList<>();

    public void addTask(Command task) {
        taskQueue.offer(task);
    }

    public void executeTasks() {
        while (!taskQueue.isEmpty()) {
            Command task = taskQueue.poll();
            task.execute();
        }
    }
}
```

### 4. 文本编辑器
```java
class TextEditor {
    private StringBuilder text = new StringBuilder();
    private Stack<Command> history = new Stack<>();

    public void insert(String str) {
        Command insertCommand = new InsertCommand(text, str);
        insertCommand.execute();
        history.push(insertCommand);
    }

    public void undo() {
        if (!history.isEmpty()) {
            Command command = history.pop();
            command.undo();
        }
    }
}
```

## 命令模式的变种

### 1. 宏命令（Macro Command）
```java
public class MacroCommand implements Command {
    private List<Command> commands;

    public MacroCommand(List<Command> commands) {
        this.commands = commands;
    }

    @Override
    public void execute() {
        for (Command command : commands) {
            command.execute();
        }
    }
}
```

### 2. 队列请求
```java
public class CommandQueue {
    private Queue<Command> queue = new LinkedList<>();

    public void addCommand(Command command) {
        queue.offer(command);
    }

    public void process() {
        while (!queue.isEmpty()) {
            queue.poll().execute();
        }
    }
}
```

### 3. 日志请求
```java
public class CommandLogger {
    private List<Command> executedCommands = new ArrayList<>();

    public void executeAndLog(Command command) {
        command.execute();
        executedCommands.add(command);
    }

    public void replay() {
        for (Command command : executedCommands) {
            command.execute();
        }
    }
}
```

## 使用命令模式的时机

### ✅ 适用场景
1. 需要将**请求调用者**和**请求接收者**解耦
2. 需要在不同的时间指定请求、排队和执行请求
3. 需要支持撤销和重做操作
4. 需要支持事务操作
5. 需要支持宏命令
6. 需要将操作的日志记录下来

### ❌ 不适用场景
1. 请求很简单，不需要额外的封装
2. 性能要求极高，命令对象会带来额外的内存开销
3. 不需要支持撤销、队列等高级功能

### 对比理解命令模式的真正价值

#### 🤔 如果没有"命令"这一层

直接由控制层，操控底层组件，需要考虑的组件，及其底层调用细节太多，这个类扩展性很差，组件也很难添加

```java
// 传统方式 - 紧耦合
class RemoteControl {
    private Light light;
    private Fan fan;
    private TV tv;
    private AirConditioner airConditioner;

    public void pressLightButton() {
        if (light != null) {
            light.turnOn();  // 直接依赖Light类
        }
    }

    public void pressFanButton() {
        if (fan != null) {
            fan.high();      // 直接依赖Fan类
        }
    }
    // 问题：每增加一种家电，都要修改RemoteControl类！
}
```

#### ✅ 有了"中间人"

新增家电只需要新增Command实现，RemoteControl基本不变，不需要关心自己最终底层操作的对象，他们都被具体的命令（实现类）给处理了，客户端想要用这些新扩展的内容时，直接创建命令实现类对象，并且注入控制层即可！

调用者是面向命令的，只负责管理、维护命令即可

```java
// 命令模式 - 完全解耦
class RemoteControl {
    private Command[] commands;  // 只知道Command接口

    public void pressButton(int slot) {
        commands[slot].execute();  // 完全不知道具体是什么
    }
}
```

## 总结

命令模式的核心价值在于：**引入"中间人"实现彻底解耦**

- **调用者**：只知道调用命令，不知道谁来执行
- **中间人**：知道调用谁，以及调用什么方法
- **接收者**：只管干活，不知道被谁调用

这种"中间人"设计让系统变得极其灵活和可扩展。遥控器可以控制任何实现了Command接口的设备，而遥控器本身完全不需要知道这些设备的存在。

**记住命令模式的精髓：通过中间人，让调用者和接收者彻底解耦！**